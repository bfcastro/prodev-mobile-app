import { View, Text } from "react-native";

const Saved = () => {
  return (
    <View className="flex-1 p-4 bg-white">
      <Text className="text-4xl font-bold text-black">Saved Screen</Text>
    </View>
  );
};

export default Saved;